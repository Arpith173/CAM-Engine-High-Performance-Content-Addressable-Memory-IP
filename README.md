# CAM-Engine-High-Performance-Content-Addressable-Memory-IP
Content Addressable Memory (CAM) — a type of memory that searches by content instead of by address. Think of it like a database lookup: you give it a value, and it tells you where that value is stored.
